<h1>ホーム</h1>

<p><%= notice %></p>

<%= link_to '投稿する', new_post_path %>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %><td>
    <td><%= link_to 'edit', edit_post_path(post.id) %></td>
    <td><%= link_to 'delete', post_path(post.id), method: :delete, data: {confirm: '本当に削除しますか？'} %></td>
  </tr>
<% end %>
</table>